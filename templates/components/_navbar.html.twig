<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">School</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        {% if app.user and is_granted('ROLE_STUDENT') %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_enrollment_available') }}">Browse Courses</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_student_grades') }}">My Grades</a></li>
        {% endif %}
        {% if app.user and is_granted('ROLE_TEACHER') %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_course_index') }}">Manage Courses</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_grade_index') }}">Manage Grades</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_statistic_index') }}">Statistics</a></li>
        {% endif %}
      </ul>
      <ul class="navbar-nav">
        {% if app.user %}
          <li class="nav-item"><a class="nav-link" href="#">Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Logout</a></li>
        {% else %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Login</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Register</a></li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
